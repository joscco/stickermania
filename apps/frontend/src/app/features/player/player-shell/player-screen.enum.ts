export enum PlayerScreen {
    CONNECTING = 'connecting',
    RECONNECTING = 'reconnecting',
    DISCONNECTED = 'disconnected',
    LOBBY_PROFILE = 'lobby-profile',
    STICKER_SPACE = 'sticker-space',
}
