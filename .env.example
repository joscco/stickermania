# Optional local runtime settings.
# The npm scripts do not load this file automatically; export values in your
# shell or use your preferred dotenv wrapper if you need them.

PORT=3001
DATA_ROOT=.data
SESSION_STORE=file
ASSET_STORE=local

# Optional local board password. Leave unset for an open local LAN host.
ADMIN_PASSWORD=
