{
  "name": "birthday-sandbox-grid",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:back": "npm run build -w @birthday/shared && npm run dev -w @birthday/backend",
    "dev:front": "npm run dev -w @birthday/frontend",
    "build:all": "npm run build -w @birthday/shared && npm run build -w @birthday/frontend && npm run build -w @birthday/backend",
    "start:party": "npm run build:all && node apps/backend/dist/index.js --serve-static",
    "start:party:admin": "npm run build:all && ADMIN_PASSWORD=master node apps/backend/dist/index.js --serve-static"
  },
  "dependencies": {
    "gsap": "^3.14.2",
    "ws": "^8.19.0"
  }
}
