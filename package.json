{
  "name": "birthday-sandbox-grid",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "frontend",
    "backend",
    "packages/*"
  ],
  "scripts": {
    "dev": "node scripts/dev-live.mjs",
    "sprite": "npm run sprite -w @birthday/frontend",
    "start": "npm run build -w @birthday/shared && npm run build -w @birthday/frontend && npm run build -w @birthday/backend && npm run start -w @birthday/backend",
    "cloud:deploy": "node scripts/cloud-deploy.mjs",
    "cloud:start": "gcloud run deploy birthday-game --image europe-west1-docker.pkg.dev/birthday-game-2026/birthday-game/birthday-game:latest --region europe-west1 --project birthday-game-2026 --allow-unauthenticated --timeout 3600 --max-instances 1 --min-instances 1",
    "cloud:stop":  "node scripts/cloud-deploy-offline.mjs"
  },
  "dependencies": {
    "gsap": "^3.14.2",
    "material-icons": "^1.13.14",
    "material-symbols": "^0.44.1"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
